import { Component } from '@angular/core';

    @Component({
        selector: 'feedback',
        template: require('./feedback.component.html'),
        styles: [require('../../../sass/feedback.scss').toString()]
    })

    export class FeedbackComponent {
        
    }   